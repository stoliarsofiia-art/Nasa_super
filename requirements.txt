# Optimized dependencies for Vercel deployment
# Removed: matplotlib, seaborn, gunicorn (not needed for serverless)
# Using specific versions to control size

flask==3.0.0
flask-cors==4.0.0
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.2
joblib==1.3.2
