# 🚀 Інструкція з розгортання на Vercel (Українською)

## Швидкий старт

Ваш проект налаштовано для безкоштовного розгортання на Vercel!

## 📋 Що потрібно

1. **Акаунт GitHub** - ваш код має бути на GitHub
2. **Акаунт Vercel** - зареєструйтесь на [vercel.com](https://vercel.com) (безкоштовно)
3. **Натреновані моделі** - переконайтесь, що папка `models/` містить файли .pkl

## 🎯 Покрокова інструкція

### Крок 1: Завантажте код на GitHub

```bash
git add .
git commit -m "Налаштування для Vercel"
git push origin main
```

### Крок 2: Підключіться до Vercel

1. Відкрийте [vercel.com/new](https://vercel.com/new)
2. Натисніть "Import Project"
3. Виберіть ваш репозиторій на GitHub
4. Натисніть "Import"

### Крок 3: Налаштуйте проект

- **Framework Preset:** Other
- **Root Directory:** ./ (залишити за замовчуванням)
- **Build Command:** (залишити порожнім)
- **Install Command:** `pip install -r requirements.txt`

### Крок 4: Розгорніть

1. Натисніть "Deploy"
2. Зачекайте 2-5 хвилин
3. Ваш API буде доступний за адресою: `https://ваш-проект.vercel.app`

## ✅ Перевірка роботи

### Перевірка здоров'я API:
```bash
curl https://ваш-проект.vercel.app/api/health
```

### Тестовий запит:
```bash
curl -X POST https://ваш-проект.vercel.app/api/predict \
  -H "Content-Type: application/json" \
  -d '{
    "orbital_period": 289.9,
    "transit_duration": 7.4,
    "transit_depth": 0.00492,
    "snr": 12,
    "stellar_mass": 0.97,
    "stellar_temp": 5627,
    "stellar_magnitude": 11.7
  }'
```

## 🔧 Що було налаштовано

### 1. `vercel.json`
Конфігурація для Vercel:
- Python 3.12
- Маршрутизація до `api/index.py`
- Максимальний розмір 250MB

### 2. `.vercelignore`
Виключає непотрібні файли:
- CSV файли з даними
- Тестові файли
- Документацію
- Скрипти

### 3. `api/index.py`
Serverless функція для Vercel:
- Flask API
- Ледаче завантаження моделей
- CORS для GitHub Pages

### 4. `requirements.txt`
Оптимізовані залежності:
- Видалено: matplotlib, seaborn, gunicorn
- Залишено тільки необхідне для API

## ⚠️ Вирішення проблем

### Помилка: "Перевищено 250 MB"

**Рішення 1:** Зберігайте моделі зовні (Google Drive, Dropbox)

```python
# Завантаження моделі з URL
import requests
import joblib

def load_model(url):
    response = requests.get(url)
    with open('/tmp/model.pkl', 'wb') as f:
        f.write(response.content)
    return joblib.load('/tmp/model.pkl')
```

**Рішення 2:** Стисніть моделі

```python
import joblib
# Зберегти з компресією
joblib.dump(model, 'model.pkl', compress=3)
```

**Рішення 3:** Оновіться до Vercel Pro (платно)

### Помилка CORS

Оновіть `api/index.py`:

```python
CORS(app, origins=["https://ваш-сайт.github.io"])
```

## 🌐 Оновлення фронтенду

Змініть URL API у вашому JavaScript:

```javascript
const API_URL = 'https://ваш-проект.vercel.app/api/predict';

async function classifyExoplanet(data) {
  const response = await fetch(API_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data)
  });
  return await response.json();
}
```

## 💰 Безкоштовний тариф Vercel

**Включає:**
- 100 GB трафіку на місяць
- 100 годин виконання функцій
- Автоматичний HTTPS
- Глобальний CDN
- Ідеально для навчальних проектів!

## 📱 Доступні ендпоінти

- `GET /` - Інформація про API
- `GET /api/health` - Перевірка здоров'я
- `POST /api/predict` - Класифікація екзопланети
- `POST /api/analyze` - Аналіз даних (те саме що predict)
- `POST /api/predict/batch` - Пакетна обробка

## 🎉 Готово!

Ваш API тепер працює на Vercel безкоштовно!

**Корисні посилання:**
- Документація Vercel: [vercel.com/docs](https://vercel.com/docs)
- Python на Vercel: [vercel.com/docs/functions/serverless-functions/runtimes/python](https://vercel.com/docs/functions/serverless-functions/runtimes/python)

## 📞 Потрібна допомога?

Якщо виникли проблеми:
1. Перевірте логи в Vercel Dashboard
2. Переконайтесь, що всі файли .pkl завантажені на GitHub
3. Перевірте розмір папки `models/` (має бути < 200 MB)
4. Подивіться детальну інструкцію в `VERCEL_DEPLOYMENT.md`

Успіхів! 🚀
